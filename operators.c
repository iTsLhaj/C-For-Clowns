
#include <stdio.h>


int main(int argc, char** argv)
{
	// OPS
	
	/* LOGIC
	 *
	 * AND -> &&
	 * NOT -> !
	 * OR  -> ||
	 * XOR -> ^
	 * 
	 */

	/* ARITHMETIC
	 *
	 * ADD -> +
	 * SUB -> -
	 * MUL -> *
	 * DIV -> /
	 *
	 */

	/* idk
	 * 
	 * INC by one ++
	 * DEC by one --
	 *
	 * x += a INC x BY a
	 * x -= a DEC x BY a
	 * x *= a MUL x BY a
	 * x /= a DIV x BY a
	 * 
	 */

	return 0;

	return 0;
}
